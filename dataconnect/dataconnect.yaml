specVersion: "v1"
serviceId: "airquality"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "airquality-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
